@model Book


<div class="card mb-2">
    <img class="card-img-top" src="@Model.Image" alt="">
    <div class="card-body">
        <h5 class="card-title">@Model.BookName</h5>
        <small class="text-muted">
            <i class="fas fa-lira-sing"><h5>Sayfa Sayısı</h5> @Model.Pages</i>
        </small>
    </div>
    <div class="card-footer text-center">
        <a asp-controller="Home" asp-action="BookDetails" asp-route-Url="@Model.Url" class="btn btn-primary btn-sm">İncele</a> @*book details altındaki product bilgisine gidiyo giderkende productid gönderiyo biz id bilgisini alıyoruz ve o id bilgisine uyan kaydı book comtroller içerisinde kullanıcılara göstericez*@
    </div>
</div> 